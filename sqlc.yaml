# sqlc configuration for hooks
version: "2"

sql:
  - engine: "postgresql"
    queries: "db/query/"
    schema: "cmd/migrations/migrations/"
    gen:
      go:
        package: "db"
        out: "db/sqlc"
